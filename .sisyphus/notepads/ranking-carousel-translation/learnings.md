# Learnings - RankingCarousel Translation

- UI text labels in `RankingCarousel` were translated to Korean.
- Used `인기 랭킹` for "Top Rankings".
- Used `전체보기` for "View All".
- Verified that `lsp_diagnostics` does not report errors after translation.
