# Learnings - Shadcn/ui Guide Creation

- Shadcn/ui in React 19 environment does not require `forwardRef` for custom components.
- Tailwind CSS 4 uses `@theme` in CSS for theme definitions instead of `tailwind.config.js`.
- OKLCH color space is preferred in modern CSS for better perceptual uniformity.
