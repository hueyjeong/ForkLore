# 📐 ForkLore 디자인 시스템

**작성일**: 2026.01.02  
**작성자**: HueyJeong (with Gemini)  
**문서 버전**: v1.0

---

## 1. 개요 (Overview)

ForkLore의 디자인 시스템은 **일관된 사용자 경험**과 **효율적인 개발**을 위해 정의된 시각적 언어와 컴포넌트 라이브러리입니다.

### 디자인 철학

| 원칙 | 설명 |
|------|------|
| **문학적 우아함** | 소설 플랫폼답게 Serif 폰트와 여백을 활용한 읽기 편한 디자인 |
| **직관적 상호작용** | Git 브랜치 개념을 시각적으로 쉽게 이해할 수 있는 UI |
| **몰입형 독서** | 독서 중 위키/지도 접근이 자연스러운 사이드 패널 구조 |
| **다크모드 우선** | 장시간 독서를 고려한 눈의 피로 저감 |

### 기술 스택

- **프레임워크**: Next.js (App Router) + TypeScript
- **스타일링**: Tailwind CSS v4
- **컴포넌트 라이브러리**: shadcn/ui (New York 스타일)
- **아이콘**: Lucide Icons
- **폰트**: Geist (Sans) + Geist Mono

---

## 2. 색상 시스템 (Color System)

ForkLore는 OKLCH 색상 공간을 사용하여 더 넓은 색역과 일관된 지각적 밝기를 제공합니다.

### 2.1 Light Mode 팔레트

| 토큰 | 역할 | OKLCH 값 |
|------|------|----------|
| `--background` | 페이지 배경 | `oklch(1 0 0)` (순백) |
| `--foreground` | 기본 텍스트 | `oklch(0.145 0 0)` (거의 검정) |
| `--primary` | 주요 액션/브랜드 | `oklch(0.205 0 0)` (딥 블랙) |
| `--secondary` | 보조 요소 | `oklch(0.97 0 0)` (밝은 회색) |
| `--muted` | 비활성/배경 | `oklch(0.97 0 0)` |
| `--muted-foreground` | 보조 텍스트 | `oklch(0.556 0 0)` |
| `--accent` | 강조 (브랜치 등) | `oklch(0.97 0 0)` |
| `--destructive` | 경고/삭제 | `oklch(0.577 0.245 27.325)` (적색) |
| `--border` | 테두리 | `oklch(0.922 0 0)` |

### 2.2 Dark Mode 팔레트

| 토큰 | 역할 | OKLCH 값 |
|------|------|----------|
| `--background` | 페이지 배경 | `oklch(0.145 0 0)` (거의 검정) |
| `--foreground` | 기본 텍스트 | `oklch(0.985 0 0)` (거의 흰색) |
| `--primary` | 주요 액션/브랜드 | `oklch(0.985 0 0)` |
| `--secondary` | 보조 요소 | `oklch(0.269 0 0)` |
| `--muted` | 비활성/배경 | `oklch(0.269 0 0)` |
| `--accent` | 강조 | `oklch(0.269 0 0)` |
| `--destructive` | 경고/삭제 | `oklch(0.396 0.141 25.723)` |

### 2.3 차트/시각화 색상

데이터 시각화 및 브랜치 트리 표현에 사용:

| 토큰 | Light Mode | Dark Mode | 용도 |
|------|------------|-----------|------|
| `--chart-1` | 오렌지 계열 | 파랑 계열 | 주 브랜치 |
| `--chart-2` | 청록 계열 | 청록 계열 | 포크 브랜치 |
| `--chart-3` | 네이비 계열 | 노랑 계열 | 병합된 브랜치 |
| `--chart-4` | 노랑 계열 | 보라 계열 | 하이라이트 |
| `--chart-5` | 주황 계열 | 적색 계열 | 경고/충돌 |

### 2.4 사이드바 전용 색상

작가 스튜디오 등 사이드바 UI에 별도 토큰 적용:

- `--sidebar`, `--sidebar-foreground`
- `--sidebar-primary`, `--sidebar-primary-foreground`
- `--sidebar-accent`, `--sidebar-accent-foreground`
- `--sidebar-border`, `--sidebar-ring`

---

## 3. 타이포그래피 (Typography)

### 3.1 폰트 패밀리

```css
--font-sans: 'Geist', 'Geist Fallback';
--font-mono: 'Geist Mono', 'Geist Mono Fallback';
```

**추가 권장 폰트** (한글 지원):
- 본문: `Noto Serif KR` (소설 본문)
- UI: `Pretendard` 또는 `Noto Sans KR`

### 3.2 타이포그래피 스케일

| 용도 | 클래스 | 예시 |
|------|--------|------|
| **페이지 제목** | `text-4xl md:text-6xl font-serif font-bold` | 히어로 섹션 |
| **섹션 제목** | `text-3xl md:text-4xl font-serif font-bold` | 인기 작품 |
| **카드 제목** | `text-lg font-serif font-semibold` | 작품명 |
| **본문 텍스트** | `text-base leading-relaxed` | 소설 본문 |
| **보조 텍스트** | `text-sm text-muted-foreground` | 메타 정보 |
| **캡션** | `text-xs text-muted-foreground` | 날짜, 조회수 |

### 3.3 소설 리더 타이포그래피

사용자 커스터마이징 가능:
- **글자 크기**: 14px ~ 24px (기본 18px)
- **줄 간격**: 1.4 ~ 2.2 (기본 1.8)
- **본문 폰트**: Serif 계열

---

## 4. 간격 및 레이아웃 (Spacing & Layout)

### 4.1 Border Radius

```css
--radius: 0.625rem;  /* 10px - 기본 */
--radius-sm: calc(var(--radius) - 4px);  /* 6px */
--radius-md: calc(var(--radius) - 2px);  /* 8px */
--radius-lg: var(--radius);              /* 10px */
--radius-xl: calc(var(--radius) + 4px);  /* 14px */
```

### 4.2 컨테이너

```css
.container {
  max-width: 1280px;
  margin: 0 auto;
  padding: 0 1rem; /* px-4 */
}
```

### 4.3 주요 레이아웃 패턴

| 패턴 | 구조 | 사용처 |
|------|------|--------|
| **그리드 4열** | `grid sm:grid-cols-2 lg:grid-cols-4 gap-6` | 작품 목록 |
| **그리드 3열** | `grid md:grid-cols-3 gap-6` | 브랜치 카드 |
| **사이드 패널** | 메인 + 고정 사이드바 (w-80) | 리더, 에디터 |
| **플렉스 헤더** | `flex h-16 items-center justify-between` | 네비게이션 |

---

## 5. 컴포넌트 라이브러리 (Components)

### 5.1 shadcn/ui 기반 컴포넌트

| 컴포넌트 | 용도 | 변형 |
|----------|------|------|
| `Button` | 액션 버튼 | default, outline, ghost, destructive |
| `Card` | 콘텐츠 컨테이너 | CardHeader, CardContent, CardTitle |
| `Badge` | 상태/태그 표시 | default, secondary, outline, destructive |
| `Input` | 텍스트 입력 | - |
| `Textarea` | 장문 입력 | - |
| `Tabs` | 탭 네비게이션 | TabsList, TabsTrigger, TabsContent |
| `Slider` | 범위 선택 | 글자 크기, 줄 간격, 회차 슬라이더 |
| `DropdownMenu` | 드롭다운 | 사용자 메뉴 |
| `Tooltip` | 도움말 | 위키 용어 호버 |

### 5.2 커스텀 컴포넌트

| 컴포넌트 | 설명 |
|----------|------|
| `Header` | 글로벌 네비게이션 (로고, 메뉴, 검색, 사용자) |
| `Footer` | 푸터 링크 및 저작권 |
| `NovelReader` | 소설 뷰어 (설정 패널, 위키 패널) |
| `NovelEditor` | 작가 에디터 (AI 코파일럿 패널) |
| `WikiBrowser` | 위키 브라우저 (문맥 인식 필터) |
| `BranchExplorer` | 브랜치 탐색기 (탭, 검색, 카드) |
| `BranchDetail` | 브랜치 상세 (Git 그래프 시각화) |

### 5.3 위키 용어 스타일

소설 본문 내 위키 연동 키워드:

```css
.wiki-term {
  color: rgb(var(--color-primary));
  text-decoration: underline;
  text-decoration-style: dotted;
  cursor: pointer;
  transition: all 0.2s;
}
.wiki-term:hover {
  text-decoration-style: solid;
  font-weight: 500;
}
```

---

## 6. 아이콘 시스템 (Iconography)

### 6.1 Lucide Icons

일관된 아이콘 언어를 위해 Lucide 아이콘 사용:

| 아이콘 | 용도 |
|--------|------|
| `BookOpen` | 로고, 위키, 읽기 |
| `GitBranch` | 브랜치/포크 시스템 |
| `Map` | 다이내믹 지도 |
| `Sparkles` | AI 기능 |
| `TrendingUp` | 추천/인기 |
| `Search` | 검색 |
| `Settings` | 설정 |
| `Bell` | 알림 |
| `User` | 사용자 |
| `Heart` | 좋아요 |
| `Eye` | 조회수 |
| `Clock` | 시간/회차 |
| `ChevronLeft/Right` | 네비게이션 |
| `Star` | 정사 편입 |

### 6.2 아이콘 크기 규칙

| 용도 | 크기 |
|------|------|
| 인라인 (텍스트 옆) | `h-3 w-3` 또는 `h-4 w-4` |
| 버튼 내부 | `h-4 w-4` 또는 `h-5 w-5` |
| 독립형 (기능 아이콘) | `h-6 w-6` |
| 장식형 (히어로) | `h-8 w-8` 이상 |

---

## 7. 반응형 디자인 (Responsive Design)

### 7.1 브레이크포인트

Tailwind 기본 브레이크포인트 사용:

| 이름 | 최소 너비 | 용도 |
|------|-----------|------|
| `sm` | 640px | 2열 그리드 시작 |
| `md` | 768px | 태블릿, 네비 펼침 |
| `lg` | 1024px | 4열 그리드, 검색 표시 |
| `xl` | 1280px | 최대 컨테이너 너비 |

### 7.2 반응형 패턴

- **모바일**: 단일 열, 햄버거 메뉴, 하단 검색
- **태블릿**: 2~3열, 네비 펼침
- **데스크톱**: 4열, 사이드 패널 고정

---

## 8. 애니메이션 & 트랜지션 (Motion)

### 8.1 기본 트랜지션

```css
transition-all    /* 모든 속성 */
transition-colors /* 색상 변화 */
transition-shadow /* 그림자 변화 (호버) */
transition-transform /* 스케일 변화 */
```

**기본 duration**: `duration-300` (300ms)

### 8.2 호버 효과

| 요소 | 효과 |
|------|------|
| 카드 | `hover:shadow-lg`, `hover:shadow-md` |
| 이미지 | `group-hover:scale-105` |
| 링크 | `hover:text-primary` |
| 버튼 | 내장 호버 스타일 |

---

## 9. 다크 모드 (Dark Mode)

### 9.1 구현 방식

클래스 기반 토글: `document.documentElement.classList.toggle("dark")`

### 9.2 CSS 변수 전환

```css
@custom-variant dark (&:is(.dark *));
```

모든 색상 토큰이 `.dark` 클래스 하위에서 자동 전환됩니다.

---

## 10. 접근성 (Accessibility)

### 10.1 포커스 스타일

```css
* {
  @apply border-border outline-ring/50;
}
```

### 10.2 권장 사항

- 모든 인터랙티브 요소에 `aria-label` 또는 명확한 텍스트
- 색상 대비 WCAG AA 이상 준수
- 키보드 네비게이션 지원
- 스크린 리더 호환성

---

## 문서 끝
