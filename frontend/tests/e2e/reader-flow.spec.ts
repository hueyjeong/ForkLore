import { test, expect } from '@playwright/test';

test('reader navigation', async ({ page }) => {
  // 1. Navigate to a novel detail page
  // await page.goto('/novels/1');
  
  // 2. Check if title is visible
  // await expect(page.locator('h1')).toBeVisible();
  
  // 3. Navigate to reader
  // await page.click('text=Read First Chapter');
  
  // 4. Verify reader content
  // await expect(page.locator('article')).toBeVisible();
});
